### API Authentication ###
# In terminal type "env" to see what environment do we have
# If want to create environment variable type "export {environment variable}={value}"
# Use "os" library to reach the environment variable by os.environ.get("{environment variable}")